# Constraints register — what binds the next step

**Read this before starting work. It is an index of triggers, not a summary of rules.**

Each row says *when* a constraint bites and *where the rule lives*. **The row is never the rule** —
open the source. A row that starts to look like a paraphrase has become a bug.

| When you are about to… | Open |
|---|---|
| Write anything that decides a trade | `CLAUDE.md` — "The one rule that governs everything"; `VISION.md` — "What the SLM does vs what generates signals" |
| Implement any part of the sequence, the entry, or the exit | `docs/RULEBOOK.md` — §2 sequence, §5 entry, §6 targets and exit |
| Read a term the engine has to compute | `docs/RULEBOOK.md` — §0 status legend, then the section that defines it |
| Pick a number the material does not pin | `docs/RULEBOOK.md` — §11 decisions, §12 open; `CLAUDE.md` — no grid searches |
| **Write any new rule into `docs/RULEBOOK.md` §1–§9** | `claude_memories/audit-hard-rules-not-in-material.md` — say where the predicate came from before writing it; a rule invented to fill a silence pre-decides what Phase 4's SLM exists to discover. `docs/PLAN.md` Phase 2a is the audit that clears the ones already there |
| **Route a term to Phase 4's SLM, or wonder what is left for it** | `docs/evidence/phase4_room_left.md` — the 2026-08-10 count of §1–§9's **21 J** rows. **Only 5 carry a D-row**, so the register is not what constrains the SLM; **7 are inert**, **3 are out of the v1 path**, and of the rest **O-20 says outright not to fill it**. Two rows are where a proposal would change something: **§7.4.1/§7.4.2** (**D-9** assigns it, **O-9** is open, and §13 keeps anything proposed there as *context*, never a step of the sequence) and **§7.3 *trapped side***, which has **no open row to land in** — opening one is the user's call. The SLM's input stays the **passage census**, never `docs/RULEBOOK.md` |
| Select a base boundary, or backtest one | `docs/RULEBOOK.md` — §2.2.8 and its engine note; then **D-34**, because the span the boundary is drawn from is re-read every bar and ends at `pos - 1` |
| **Add a fourth way the PTB walk can end, or reach for a timeout** | `docs/RULEBOOK.md` §5.3.4a — it is a **closed list of three** and there is still **no timeout**: a **fill**; any of **§5.4.7a–c** (`ORDER_CANCELLED`, which also drops open positions); and **§5.3.4b** / **D-35**, the leg resuming past the Step 3 extreme (`ORDER_VOIDED`, which touches the working order and nothing else — no position closed, no reset, and a continuation PTB may anchor after that bar). All three live in `stoic/entry.py`. **D-35 is a no-op in the current engine and that is expected, not a bug:** `trigger ≤ extreme` by construction, so a bar reaching the extreme has already filled, gaps included (**D-22** fills at the open) — 0 fires over 7,860 NQ 5m bars. `tests/test_entry.py` case 18 pins it; if it ever fires, that is a decision to make, not drift |
| Decide whether a trade **or a pending order** is still alive | `docs/RULEBOOK.md` — §5.4.7–§5.4.7c (three conditions, any one ends it) and the engine note under them; §5.3.4a for what ends an unfilled order; decisions **D-24** and **D-17**; §12 row **O-15** |
| Move a stop after entry | `docs/RULEBOOK.md` — §5.4.5 and the engine note on 5.4.4/5.4.5; decision **D-25**; §6.5b and **D-26**. There is exactly one stop move, it is not a trail, and an inside candle never moves it. **The labelled material contains a second kind of move and it is not a rule:** on `T-A2` the trader moved his stop to **lock +1R** (*"i will lock in plus one r"*, `PTBV @ 01:26:50`) and that is what produced the printed *"+1R"*. It is discretionary, of the same class as climax (§4.3) and the lower-high cue (**D-26**), it is **not written into §1–§9** and gets **no D-row** — `claude_memories/audit-hard-rules-not-in-material.md`. **Phase 6 must not score that exit**: `docs/evidence/labels/2026-07-31_T1_T2.yaml`, `management_observed` |
| Reach for a "price made a lower high" test after entry | `docs/RULEBOOK.md` §6.5a–§6.5b and decision **D-26** — it is a **cue, not an invalidation**, and an inside candle never counts as one. §5.4.7 is the closed list of things that end a trade |
| Cite a diagram, or lean on two sources agreeing | `docs/AUDIT-2a.md` — F-1: `DIA-P`, `DIA-L` and `step-1-2-3.svg` are one file. Check `md5` before calling two sources independent |
| Compute R, or place a stop | `docs/RULEBOOK.md` — §5.4.1–§5.4.2 and the engine note on 5.4.4/5.4.5. R is fixed once at fill; **5.4.4 has no floor** — do not reintroduce one |
| Decide which bar the entry order sits on | `docs/RULEBOOK.md` — §5.3.3a–c (what a PTB candidate *is*, and the two tests that are **not** it) and the engine note under them; §5.3.4–§5.3.6 (no bar cap; the order re-anchors) and §5.3.4a (what ends the walk); §5.2.8–§5.2.8a (an inside bar is never the anchor, and what *inside* is measured against) |
| Decide where a pullback starts, or reach for a distance-to-the-MA test | `docs/RULEBOOK.md` §5.2.1a and decision **D-28** — both extremes must move against the direction, measured against the parent bar; inside and outside bars do not open a pullback. Also the engine note on §5.3.3a and the `docs/PLAN.md` Phase 5 layer table. A pullback is a property of a **leg**, owned by **L1** — a layer that has to invent a predicate is usually reaching past a layer that already owns it |
| Emit a second signal while a direction is still running | `docs/RULEBOOK.md` — §2.5 and §3.7; decision **D-21** |
| Fill an order, or decide what a gap through the trigger costs | `docs/RULEBOOK.md` — §5.3.7–§5.3.10 and the engine note under them (R comes from the fill, not the trigger) |
| Reach for a lookback or tolerance on an MA filter | `docs/RULEBOOK.md` — §7.1.7 and the note under it |
| Gate a signal on the 50 or the 200 SMA | It is already built — `stoic/gating.py` (**L4**). Read its docstring: it fixes **five** conventions (an exact tie with either MA blocks; a NaN SMA blocks; `fast_chart` has **no default**) and names every filter it deliberately does **not** implement. It holds **no threshold** and must stay that way. The 200 rule is **symmetric since D-31** (a short is blocked at or above the 200 as a long is blocked at or below it — the bullish half cited, **the mirror a decision**) and **fast-chart-only**, which is cited and separate: `SCALP @ 22:45` licenses the `50 → 200` long on a higher timeframe, so the caller declares the chart and the engine may not guess |
| Reach for the no-edge zone as a **filter you can compute** | `docs/RULEBOOK.md` §12 row **O-19** — §7.4.3 was the corpus's one *"mechanical"* instance and it is not one. `CST @ 00:22:05` names **PDC** a daily level and PDC sits **inside** the PDH/PDL range, so *strictly-between-blocks* forbids what the same passage licenses. Three readings, none stated; the human opened the row rather than pick one. **L4 does not implement it** |
| **Gate a signal on R, on a trapped side, or on HTF alignment** | `docs/RULEBOOK.md` §7.5.4 and **O-10** (record R, do **not** gate); §7.3 *Trapped side* (**J** — inventing it is what `claude_memories/audit-hard-rules-not-in-material.md` forbids); §7.5.5 / **D-27** (HTF alignment raises the confluence score and **never blocks** — it is L5's, not a gate). Also §7.1.4: the 200 SMA rule now gates **both** directions on fast charts (**D-31**) — the material states the bullish case only and the mirror is the human's, so do not read the bearish half as cited |
| Reach for a rule from the daily-templates / signal-days / chop-zone material | `docs/RULEBOOK.md` — §13 |
| **Label a fixture off a marked chart, or go looking for its stop** | `docs/PHASE3.md` — three label classes (`taken` / `named` / `no_opportunity`), per-field `provenance`, one YAML per session. **`named` and `no_opportunity` split on who prevented the trade**, and they score differently. Then `docs/RULEBOOK.md` §10.10: **no §10 chart draws a stop**, so a stop gets both readings and neither is adopted — they disagree by **12.25** points on `LT`. **Check the Type before recording an outcome** (`LT3`/`LT4` is held past the flatten on a 5m chart and no §9 Type is both). **There is no `verify_labels.py`** — nothing re-checks a label when a bar or a reading changes |
| **Treat `PTBV` as one session, or place a narrated setup on a bar** | **It is two sessions, and both are reconciled** — `docs/evidence/ptbv_session_map.md`. The clock jumps **backward** at the seam and the narration turns over inside the gap. **`PTBV` created no label id**; its narration lives in the two session files under `ptbv_narration`. **Never interpolate video time onto a bar** — the recording is cut, and 00:34:58 → 00:44:58 spans 2h37m of wall clock. Read the clock off the nearest keyframe (**its x shifts with the order panel**) or bracket between two measured ones |
| **Recover a stop from a dollar figure (§10.10), or reach for the `~$1,000` risk unit** | **It is a SUGGESTION, not a reading — the user's call, 2026-08-11 — until it is tested against a second instrument.** The $1,000 is a **cap he sizes under**, not a constant he hits, and **$2/point is MNQ** while our bars are NQ. So a `by_r_label` distance **may never contradict §5.4.1 / D-18**, and nothing in `stoic/` reads a dollar figure. `docs/evidence/labels/2026-07-31_T1_T2.yaml`, `stop_recovery` |
| **Date a marked chart, or wonder which session one is** | It is already done — `docs/evidence/fixture_dating.md`, re-run with `scripts/date_marked_chart.py`. Match the four printed right-axis values as a **sorted multiset**; the chart never says which label is which average. **One sharp minimum is a candidate, not a date** — `docs/PHASE3.md` needs a second, independent agreement. **Anchor on a level you measured off the plot, not on a printed number** |
| **Write a correction, a caveat or a chain of reasoning into `docs/RULEBOOK.md` §10** | **Don't. §10 records what the artifact shows; our reasoning goes to `docs/evidence/`.** That is the disposition every row here already uses — *"reported, not corrected, editing §10 is the user's call"* — and on 2026-08-10 it was breached: four §10.9 edits carrying our own measurement and argument were written and then **reverted in full**, the findings staying in `docs/evidence/` where they already were. Two reasons it matters. Reasoning in §10 **reads as rule text**, and §10 is a section the SLM may later be pointed at, which is the contamination `claude_memories/audit-hard-rules-not-in-material.md` exists to prevent. And a §10 row is **not a changelog** — §0 says so of the whole file. A *factually wrong* §10 row is still the user's to fix, not ours |
| **Read a §10 chart's printed number as a price** | It may be an **indicator's axis label**, and this has now caught **four** §10 numbers. §10.7's *"Stop 28,540.75, the PTB low"* is within 0.04 of that chart's 20 SMA, while the PTB bar's low is 28,529.50. §10.2's *28,482*, §10.8's *28,473.81* and §10.9's *28,471.53* are the **200 SMA** (`T1`, `T2`) and the **10 SMA** (`LT3`/`LT4`) at each chart's last bar — the **drawn** `Jun LCOM` line measures 28,471.6 against our 28,472.00. **Check the value against our SMAs at that chart's last bar before reading it as a level**, and prefer a level you measured off the plot. `docs/evidence/fixture_dating.md`; not corrected in `docs/RULEBOOK.md`, editing §10 is the user's call |
| **Read a marked chart's 1/2/3 labels as the count's bars** | **Run the two-render test per fixture; the answer differs between them.** On `T1`/`T2` the x-order is **not** the count order, so no step bar is labelled from the numerals (`docs/evidence/labels/2026-07-31_T1_T2.yaml`, `count_numerals`). On `LT3`/`LT4` it **is**, and `2` and `3` land on the same bar in both renders while `1` slips one because its glyph centre falls on a slot boundary (`2026-07-30_LT3_LT4.yaml`). Either way the numerals are hand-placed in whitespace and their y-positions are annotation heights, not prices — a numeral marks a **region**, and its bar is a reading. `LT` and `NQ3` recorded theirs as `read` before the test existed |
| **Read a level drawn on a marked chart as a base's close extreme** | **Measure it against both readings before you assume.** On `LT3`/`LT4` the drawn base boundary reproduces the base's highest **high** to 0.01 and misses its highest **close** by 5.5 points, and the `ptb` line reads the same way — while **D-30** selects the base's **close** extreme, on the §7.3 / **D-8** closes-not-wicks precedent. That is **one session** and an observation, not a change: `docs/evidence/labels/2026-07-30_LT3_LT4.yaml`, `observations`. Do not edit **D-30** off it, and do not quietly implement the high either — §11 is the user's |
| **Resolve a mark on a marked chart to a bar** | It is already built — `scripts/fit_marked_chart.py`. Fit the artifact, do not eyeball it. **Read the comb's pixel residual as a fraction of the spacing, then the price residual**: near *half* the spacing the slot count is wrong, though a large absolute value alone condemns nothing. Anchor on the last in-progress candle and **sweep the neighbours** — the price residual has separated the right anchor from ±1 bar by more than 15× on every fixture. `LT` is a different theme and returns zero candles. Numbers: the `calibration` blocks in `docs/evidence/labels/` |
| **Blame a chart-fitting failure on the CME maintenance break** | **Don't — it was measured and it is not that.** Our 5m frame holds no bars between 17:00 and 18:00 ET either, so `fit_marked_chart.py`'s `last_index - k` walks across the break correctly and needs no timestamp walk. `LT3`/`LT4`'s 16.95 pts sd was **four candles that draw no body pixels** (dojis, and candles under the shaded session boxes), each leaving a two-slot gap the old `span / median_gap` seed could not see. `docs/STATE.md`, this file and the script's docstring all carried the wrong cause for two days on the strength of one plausible-sounding story that nobody measured — `VISION.md`, *Evidence*: check the source artifact |
| **Read bars dated after 2026-06-10, or wonder where they came from** | `docs/STATE.md` Open, and `scripts/merge_signal_bars.py`'s docstring. They come from the live signal system's `signals.db`, **not** Databento's own OHLCV — rows carry `source = signals_db`. 25 of 3,240 overlap bars disagreed with ours, all inside the capture's bring-up. **`2026-06-11` → `2026-06-19` is a real hole** (as low as 9 bars of 1,380), named in Gate E; exclude or flag it exactly as for 2025-11-28. Databento's **historical** API is not available here — the key is live-feed only |
| **Extend or rebuild the bar spine** | `scripts/merge_signal_bars.py` and `scripts/normalize_historical_bars.py`, then `scripts/check_bar_spine.py`. **Do not extend `{NQ,ES}_1h.parquet` from our own 1m** — those files are the independent series Gate A compares against, so rebuilding them makes the gate compare us to us. Read `clip_to_comparable_span`'s docstring before touching Gate A: the vendor's final bar is a **partial bucket**, not a mismatch |
| Reach for a previous-week level | `docs/RULEBOOK.md` §7.3 — `PWC` / `PWH` / `PLOW` and the week-boundary note (`stoic/bars.py`, ISO week over CME trading days); §12 row **O-16** for the one that is *not* defined |
| Report how well the engine reproduces the method | `CLAUDE.md` — never conclude from small n; `VISION.md` — Evidence |
| **Reconcile an engine emission against a label, or reach for a tolerance** | `docs/PHASE6.md` §4 — exact bar, no tolerance anywhere, the user's call 2026-08-11. `stoic/fidelity.py` holds no number and must stay that way |
| **Read the unlabelled-emission count as a false-positive rate** | `docs/evidence/phase6_reconciliation.md` §3 — the label set is not exhaustive, so it is not one |
| **Add or change a `phase6_scope` block** | `docs/PHASE6.md` §2 — additive only, `from:` must resolve, and Gate 0 in `scripts/reconcile_labels.py` refuses the run if a block disagrees with the field it names. A block that disagrees is a bug in the block |
| **Write a hand-built fixture standing in for a real engine frame in `stoic/fidelity.py` or its tests** | Match the engine's real schema, not a guess at it — this class of defect occurred **three times** in Phase 6. `stoic/entry.py:471` (`_PAYLOAD_COLUMNS`) has **no `anchor_ts`**; the anchor is `anchor_pos`, an `Int64` position. `stoic/emission.py:498-527` types every timestamp column `bars.index.dtype`, so a null arrives as `NaT`, never `None`. And the engine's event enums are `StrEnum`s whose values are **lowercase** (`stoic/emission.py:230`, `stoic/entry.py:64`) — comparing `.astype(str)` against an uppercase name matches nothing on real data while a hand-built fixture using the uppercase name keeps passing |
| Resample or aggregate bars | `claude_memories/databento-ohlcv-buckets-by-ts-recv.md`; `stoic/bars.py` docstring |
| Touch a timestamp, a session boundary, or the flatten cutoff | `VISION.md` — Timestamps; `stoic/sessions.py` docstring; `claude_memories/tz-aware-day-arithmetic.md` |
| Read bars for a date near 2025-11-28 | `claude_memories/historical-bars-2025-11-28-outage.md` |
| Emit a signal record | It is already built — `stoic/emission.py` (**L5**). Read its docstring before adding a field: it fixes seven conventions and names every filter and target it deliberately does **not** implement. Behind it: `VISION.md` — "What a signal actually is"; and `docs/RULEBOOK.md` §5.3.10's engine note, which adds a field the schema omits: store the **trigger and the fill**, because they differ on a gap and only the fill sets R (§5.4.2) |
| **Decide which bar a gate or a confluence condition is read on** | `docs/RULEBOOK.md` **D-32**. The answer is the **PTB anchor bar**, not the fill bar — **D-19** makes the gate a *close* read while a fill is *intrabar* (§5.3.7), so reading either at the fill bar is **lookahead**. `stoic/gating.py` is a pure evaluator that deliberately leaves the choice to its caller; `stoic/emission.py` is that caller and passes `rec.anchor_pos`. D-32 also records the consequence — on a **fast** chart a passing gate already implies both §7.1.1 conditions, so the score varies only with the HTF input there |
| **Compute the confidence / confluence score** | It is already built — `stoic/emission.py`, and the combination is closed as **D-33**: a **plain count**, *k* of 3, every condition also recorded individually. **Weights were rejected** as invented numbers. The inputs are the rulebook's own conditions (§7.1.1 with §7.1.3, and §7.5.5 / **D-27**), read at the **D-32** bar. **Keep §7.1.1 (the read) distinct from §7.1.2 (the gate, D-19, L4's)** |
| Project a fib target | `docs/RULEBOOK.md` §6.2–§6.3c and **D-6** / **D-20** — the ratio is 2.618 and the anchor swing is Step 2 (the first pullback), but a trend-extension tool takes **three** points and no rule pins them. `stoic/sequence.py` exposes `step1_pos`, `step2_swing_pos`, `step2_swing_price` and `base`, so the data is there and only the reading is missing. **TP1 (§6.1) is fully defined and does not depend on this** |
| **Track a signalled trade to an outcome, or reach for an exit rule** | `docs/PHASE7.md` §2 (the outcome list), §3 (the three 1m windows) and §3a (ordering inside a bar). **The windows compose; they are not alternatives** — compute the live span first, then ask what it reached; an empty span is **`not_taken`**, never a coverage failure. **There is no `break_even` outcome** (TP1 is the same frozen extreme D-25 triggers on, and TP1 is a full exit). **A §5.4.7 exit is read from L2, never L3.** **`ORDER_VOIDED` (D-35) is not an exit** |
| **Pin a rule, a class or a count "at zero"** | **Name the producer you are observing, or you have pinned nothing.** `tests/test_entry.py` case 18 pins **D-35**'s `ORDER_VOIDED` at zero and is a real tripwire, because `stoic/entry.py` can construct one. Phase 7 copied the form and pinned an `Outcome` member **no code path could construct** — true whatever the engine did, shipped as coverage. `claude_memories/pin-at-zero-needs-a-producer.md` |
| **Read Phase 7's outcome counts as performance** | **Don't** — `CLAUDE.md`, never a verdict, never projected. `docs/PHASE7.md` §8 forbids the report from carrying expectancy, win rate, average R or drawdown, and the absence is a requirement, not an omission (Phase 9 owns that measurement). Before reading `tp1` counts as a hit rate, read the structural fact in `docs/STATE.md`: **TP1 is the same near level D-25 moves the stop to break-even on**, so a target that close being reached often is geometry |
| **Reach for `past_flatten` to find the flatten bar** | **Use `contains_flatten`.** `past_flatten` is False for every 5m bar by construction — the cutoff sits in a 2-minute window no 5m bar can start in. Then `docs/PHASE7.md` §4 for the three things Phase 7 got wrong there: the exit prices off the 1m bar **ending** at the cutoff; a **holiday early close leaves no containing bar** and flattens at the session's last; a fill at or after the cutoff is **`not_taken`**. `SignalType.POSITION` never flattens |
| **Infer a bar span from an index** | Call **`stoic.sessions.infer_bar_span`** — the repo already owns this, and it is the **mode** of consecutive diffs for a reason: a median averages the two middle diffs on an even count, so a short frame straddling the 17:00–18:00 ET maintenance break reports 35m where the span is 5m. `stoic/bars.py` holds the `BAR_SPAN` table if you know the timeframe already. Phase 7 briefly shipped a third implementation with a median and a hardcoded 5m fallback; `docs/CONSTRAINTS.md`'s own layer rule caught it — *a layer that has to invent a predicate is usually reaching past a layer that already owns it* |
| Write to the ledger | It is already built for v1 — `scripts/forward_test.py`, `docs/PHASE7.md` §5. Append-only, one file per Type; a trade closing is a **new row**, never an edit, and the fold is the truth. **Idempotency is the engine's** (`signal_id` is deterministic), which is why **the frame start is pinned and a disagreement refuses the run**. **A torn tail is truncated before the next append** — appending onto it silently swallows the next batch and then bricks the file. Behind it: `VISION.md` — Trade ledger |
| Decide where a run artifact goes | `claude_memories/artifact-locality.md` — and if a decision will **cite** it, it is evidence, not a run artifact: `docs/evidence/`, tracked. `.artifacts/` is gitignored and reaches one disk |
| Decide which engine layer a rule belongs in | `docs/PLAN.md` Phase 5 layer table and the correction under it — **a layer that has to invent a predicate is usually reaching past a layer that already owns it.** L3 defining a pullback atomically is what O-14 was; check the layer below before writing the predicate |
| Add a citation, or add a source to the corpus | Run `scripts/verify_citations.py`, and add the key to its `SOURCES` map in the same change — an unregistered key reports as *unknown citation key*, not as an error anyone will read |
| Compute an MA, an inside bar, a pullback boundary, or an HTF level | It is already built — `stoic/indicators.py`, `stoic/candles.py`, `stoic/structure.py`, `stoic/levels.py`. Read the module docstring before reimplementing; each names its rulebook section and the conventions it fixed |
| Anchor a PTB, fill an entry, place the stop, or move it to break-even | It is already built — `stoic/entry.py` (**L3**). Read its docstring: it fixes three conventions (the strict fill test among them) and states the two per-bar orderings that are load-bearing — the fill is checked **before** L2's events, and a `RESET` disarms without cancelling a working order (**O-15**, §5.3.4a). It holds **no threshold** and must stay that way |
| **Write a threshold, a fraction or any tuned number into engine code** | `stoic/judgment.py` — that is the **only** module allowed to hold one, and every number in it names the §11 D-row that authorised it. `stoic/sequence.py` is threshold-free **by construction** and must stay that way: it takes predicates, it does not contain them. A number appearing anywhere else is the bug this separation exists to make visible |
| **Change what counts as a base, or reach for a compression / MA-proximity / "breakouts inside" test** | It is already decided and built — `docs/RULEBOOK.md` §2.2.5a and **D-34**, implemented as `find_base` in `stoic/judgment.py`. Read the D-34 row before adding a clause: it records which of **D-3**'s three it **dropped and why**, and which rival constructions were **rejected** — so a test you are about to add may already have been considered and refused. Read the `find_base` docstring for the two conventions it fixes. It was chosen with **no eval set** (§10's marked charts are the natural one — **Phase 3**), which is why `decided_judgment(find_base=...)` stays overridable |
| **Read a `BASE_SELECTED` event as the base that was traded, or assume the base is fixed once selected** | It is neither, since **D-34** — see its §11 row's *structural consequences*, and `stoic/sequence.py`'s Step 2 block. The event fires once and the span keeps growing behind it, so **`STEP_3_BREAK` carries the span and the Step 2 swing (D-20) that actually drove the break**; §2.2.8 is enforced by the span ending at `pos - 1`, **not** by F5, which is vacuous under this construction and survives only for injected predicates |
| Reach for §2.2.7's **sloping** boundary | `docs/RULEBOOK.md` §2.2.7 and open row **O-18**. **D-30**'s horizontal default is always available, so nothing is blocked — but *when* a base edge is a trend line is undecided, and an implementation may not pick it silently. Supply it by injecting a different `select_boundary`; `Boundary` is a Protocol and `level_at(pos)` already carries any shape |
| Write any code at all | `coding_rules.md` |
| Call something an error | `VISION.md` — Evidence: check the source artifact, not the system's other output |

The two standing directives that apply regardless of strategy — see `CLAUDE.md`:

- Never conclude from small n. Report counts, not verdicts, and never project direction.
- No parameter grid searches for "the best cell". Where the material genuinely underdetermines a
  number, the human decides and it is recorded as a strategy decision.

Rows get added as decisions close. `docs/RULEBOOK.md` §11 is where a closed decision lives; §12 is
where an open one waits. Point at the source; do not paraphrase it.
