# Current state — what is true right now

**Updated: 2026-07-29.** One file, overwritten in place.

## Running now

Nothing. The prior signal-engine/backtest/ledger/dashboard build was archived to the
`stoic_legacy` branch and stripped from `main` to start over with a simpler stoic strategy — see
`git log stoic_legacy` and `git show stoic_legacy:CLAUDE.md` for what it was.

## Next, in order

1. Replace `edu/` with new source material (videos/PDFs).
2. Transcribe and read the new material.
3. Use it to build an SLM that helps derive the rulebook.

No architecture, ADRs, or parameter decisions carry over from `stoic_legacy` — this is a fresh
start with limited information so far.

## What does not belong in this file

Completed work. This file says what is true now; `git log` says what happened.
