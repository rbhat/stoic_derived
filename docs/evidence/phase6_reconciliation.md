# Phase 6 — reconciliation

Design: `docs/PHASE6.md`. Generated by `scripts/reconcile_labels.py`;
§2 is hand-written. **Counts, never verdicts** (`CLAUDE.md`).

## Run parameters

| | |
|---|---|
| instrument | `NQ` |
| frame | `5m` |
| window | `2026-06-22 -> 2026-08-04` |
| bars | `8784` |
| type | `scalp` |
| htf | `None -- confluence tops out at 2 of 3 (D-32's recorded consequence)` |
| judgment | `decided_judgment() -- D-29, D-30, D-34 defaults` |
| matching | `exact bar, no tolerance (docs/PHASE6.md §4)` |
| data holes | `2025-11-28 and 2026-06-11->06-19 both sit outside this window` |
| warm-up | `L4 needs 50 bars for any signal and 200 for a long on a fast chart` |

## 1. Per-label reconciliation

10 labels, 2 matched.

### LT-A1 — taken, short

- session: `2026-07-27`
- matched: **False**
- engine event: `None` at `None`
- nearest same-direction emission: `2026-07-27 13:55:00+00:00`, **+3 bars**
- trigger: unscoreable — no emission on the label's bar
- stop: unscoreable — no emission on the label's bar
- stop_distance: unscoreable — no emission on the label's bar
- tp1: unscoreable — no emission on the label's bar

### LT-B1 — named, short

- session: `2026-07-27`
- matched: **True**
- engine event: `ptb_anchored` at `2026-07-27 13:50:00+00:00`
- anchor bar matched: **True**
- trigger: +0.00 (label 28446.50, engine 28446.50)
- note: the engine filled this order -- correct-but-not-taken, never a false positive (docs/PHASE3.md §2)

### LT34-A1 — taken, long

- session: `2026-07-30`
- matched: **False**
- engine event: `None` at `None`
- trigger: unscoreable — no emission on the label's bar
- stop: unscoreable — no emission on the label's bar
- stop_distance: unscoreable — no emission on the label's bar
- tp1: unscoreable — no emission on the label's bar

### LT34-M1 — taken, long

- session: `2026-07-30`
- matched: **False**
- engine event: `None` at `None`
- trigger: unscoreable — no emission on the label's bar
- stop: unscoreable — no emission on the label's bar
- stop_distance: unscoreable — no emission on the label's bar
- tp1: unscoreable — no emission on the label's bar

### LT34-M2 — taken, long

- session: `2026-07-30`
- matched: **False**
- engine event: `None` at `None`
- trigger: unscoreable — no emission on the label's bar
- stop: unscoreable — no emission on the label's bar
- stop_distance: unscoreable — no emission on the label's bar
- tp1: unscoreable — no emission on the label's bar

### PTBV30-N1 — no_opportunity, long

- session: `2026-07-30`
- matched: **False**
- engine event: `None` at `None`
- note: the engine never anchored an order on this bar

### T-A1 — taken, short

- session: `2026-07-31`
- matched: **False**
- engine event: `None` at `None`
- nearest same-direction emission: `2026-07-31 03:10:00+00:00`, **-138 bars**
- trigger: unscoreable — no emission on the label's bar
- stop: unscoreable — no emission on the label's bar
- stop_distance: unscoreable — no emission on the label's bar
- tp1: unscoreable — no emission on the label's bar

### T-A2 — taken, short

- session: `2026-07-31`
- matched: **False**
- engine event: `None` at `None`
- nearest same-direction emission: `2026-07-31 03:10:00+00:00`, **-141 bars**
- trigger: unscoreable — no emission on the label's bar
- stop: unscoreable — no emission on the label's bar
- stop_distance: unscoreable — no emission on the label's bar
- tp1: unscoreable — no emission on the label's bar

### T-B1 — taken, long

- session: `2026-07-31`
- matched: **True**
- engine event: `suppressed` at `2026-07-31 16:50:00+00:00`
- blocked_by: `into_200`
- anchor bar matched: **True**
- trigger: +0.00 (label 28349.75, engine 28349.75)
- stop: +0.00 (label 28308.50, engine 28308.50)
- stop_distance: -1.17 (label 42.42, engine 41.25)
- tp1: unscoreable — phase6_scope.tp1 is null
- note: prices recovered from L3's ENTRY_FILLED -- a SUPPRESSED row carries none

### NQ3-A1 — taken, long — **CIRCULAR reference, nothing is drawn from this row**

- session: `2026-08-03`
- matched: **False**
- engine event: `None` at `None`
- nearest same-direction emission: `2026-08-03 03:20:00+00:00`, **-130 bars**
- trigger: unscoreable — no emission on the label's bar
- stop: unscoreable — no emission on the label's bar
- stop_distance: unscoreable — no emission on the label's bar
- tp1: unscoreable — no emission on the label's bar

## 2. Divergences

*Hand-written, not generated. Each divergence is triaged as a **specification bug in the
engine**, a **missing rule in `docs/RULEBOOK.md`**, or **out of v1 scope** — and the third names
the row that already put it out of scope.*

**Read this section under `CLAUDE.md`'s standing rule.** n = 10, across four sessions, every one
a single instance. Counts below are counts. **No direction is projected from them**, and nothing
here says the method does or does not work — the question is only whether our implementation
generates the trades the method calls for.

**One mechanism governs seven of the eight unmatched rows, and it is now identified rather than
merely localised.** A first pass reported only that L2 confirmed Step 3 in RTH on all four sessions
while L3 anchored a PTB on just two of them. Tracing the entry machine's own state per bar gives
the cause: **§5.4.7's invalidation fires between the Step 3 confirmation and the entry, every
time.** In each case the condition is `invalidated_boundary_close` — **§5.4.7c**, a close beyond
the Step 2 boundary against the trade direction, which §5.4.7 applies to *"an open position and to
a pending setup alike."*

| session | Step 3 confirmed | invalidated | gap | order existed? |
|---|---|---|---|---|
| 07-30 bullish | 18:05 | 18:15 §5.4.7c | 2 bars | no |
| 07-30 bullish | 20:15 | 20:50 §5.4.7c | 7 bars | no |
| 08-03 bullish | 13:15 | 13:25 §5.4.7b + §5.4.7c + reset | 2 bars | no |
| 08-03 bullish | 14:55 | 15:05 §5.4.7c | 2 bars | no |
| 07-31 bearish | 14:05 | 14:25 §5.4.7c | 4 bars | **yes** — `ORDER_CANCELLED` |

**The 07-31 row is the engine applying the rule exactly as written.** A PTB anchored at 14:20, a
close beyond the boundary at 14:25 cancelled it (§5.3.4a's second of three ways the walk ends), and
the labelled entries follow at 14:40 and 14:55. So `T-A1` and `T-A2` are **not** an engine failure
to see the setup: the engine saw it, armed it, and then invalidated it on a rule, 3 and 6 bars
before the trader entered. What is unresolved is *which* side is right — and that is now a narrow
question about one line, not a general one about the engine.

**The four rows where no order existed raise a separate question the rulebook does not settle.**
There, §5.4.7 fired with **no open position and no working order** — only an *armed* machine
waiting for a pullback to open. Its effect in `stoic/entry.py` is to clear `armed`, so the setup
dies before a PTB can ever anchor. §5.4.7 scopes itself to *"an open position and to a pending
setup alike"*, and the engine note repeats it: *"evaluated per bar on an open position and on a
pending setup."* **Whether "Step 3 confirmed, no pullback opened yet" is a pending setup is not
stated anywhere.** Both readings are defensible — the setup is arguably pending from confirmation,
or arguably only once an order rests — and this run does not decide it. It is the same shape as
**O-15** and **O-17**: noticed, not decided. **Do not resolve it by editing the engine.**


### LT-A1 — the engine anchored no order on the label's bar; its nearest emission belongs to a different setup

- **What the engine did:** no L3 record at `13:40`. Its first RTH bearish anchor is `13:50` at
  trigger `28446.50` — which is **`LT-B1`'s setup, not this one** (`LT-B1` matched exactly, below).
  §1 reports a nearest same-direction emission `+3 bars` away, at `13:55`; that row is the *fill*
  of `LT-B1`'s order, so the `+3` is not a near-miss of this label.
- **What the label says:** anchor `13:35`, entry `13:40`, trigger `28525.75` (`ptb.trigger.bars`,
  provenance `read`), stop `28613.75` by the PTB extreme.
- **What the trace adds:** the engine emitted `step_3_break` at `13:35` — **the label's anchor
  bar** — and `step_3_confirmed` at `13:40` — **the label's entry bar, exactly.** It then anchored
  a PTB at `13:50` and filled at `13:55`. So the engine and the label agree on where the sequence
  is; they disagree on **when you may enter it**. The trader entered on the confirmation bar
  itself; the engine requires a pullback to open after confirmation (§5.2.1a / **D-28**), anchors
  the PTB on it, and fills on a trade through its extreme — three bars later here.
- **Triage:** **out of v1 scope, pending one artifact check.**
- **Basis:** §5.5 / **D-11** — the anticipatory entry. `docs/STATE.md` already records that it
  *"appears in the labelled material and the engine must not emit it"*, which is exactly this
  shape: an entry taken at Step 3 confirmation rather than on the PTB that follows it. The check
  that would settle it is whether the artifact's `28525.75` is a PTB extreme at all or the Step 3
  break level. **If it is D-11, the engine is right not to reproduce this row.**

### LT34-A1, LT34-M1, LT34-M2 — three labelled entries on a session where L3 anchored nothing in RTH

- **What the engine did:** `2026-07-30` produced **0 L3 records** between 13:00 and 21:00 UTC,
  against 29 L2 events and 2 `step_3_confirmed` in the same window. No order was anchored, so no
  fill was possible at `19:55`, `13:35` or `14:15`.
- **What the label says:** `LT34-A1` entry `19:55`, trigger `28241.75` (`ptb.by_rule`, derived);
  `LT34-M1` and `LT34-M2` carry **no `ptb` at all** — their scope blocks are `anchor_bar: null`,
  `trigger: null`, `stop: null`, and declare only direction and entry bar as scoreable.
- **Triage:** unresolved — the shared diagnostic above, not a per-label finding.
- **Basis:** `LT34-A1`'s *exit* is separately out of v1 scope (held past the 13:58 PT flatten; no
  v1 Type runs a 5m sequence and survives the cutoff — `docs/STATE.md`, settled 2026-08-10). That
  row covers the exit **only**. The label's own text scopes Phase 6 here to *entry, trigger and R*,
  so the missed entry is in scope and is not excused by it.

### PTBV30-N1 — the class whose expectation the engine met, by a route the label did not describe

- **What the engine did:** no order anchored at the narrated `17:25` bar, and no fill anywhere in
  that session's RTH. §1 records `matched: False` with the note *the engine never anchored an
  order on this bar*.
- **What the label says:** class `no_opportunity` — a working buy stop the market never traded
  through, *"ptb fails back below no entry"*. Its `phase_6_expectation` is: identify the setup,
  emit **no fill**.
- **Triage:** **partially met — recorded, not credited.**
- **Basis:** the engine emitted no fill, which is half the expectation; it also never identified
  the setup, which is the other half. `docs/PHASE3.md` §2 makes this the one class where absence
  is the right answer *and* the bars can settle it, so an absence produced by never having seen
  the setup is **not** the same result as an absence produced by the trigger holding. Scoring the
  first as a match would be the false confirmation this class exists to prevent.

### T-A1, T-A2 — the bearish walk died 15 and 35 minutes before the labelled entries

- **What the engine did:** one bearish order anchored at `14:20` (trigger `28157.00`) and was
  **cancelled at `14:25`**. No bearish L3 record follows it for the rest of the session. The
  labelled entries at `14:40` and `14:55` therefore have no order to fill. Both labels' nearest
  same-direction emission is ~138 bars away, in the prior Asia session — i.e. nothing nearby.
- **What the label says:** `T-A1` entry `14:40`, `ptb` null (the drawn `ptb` line starts at the
  10:50 bar and belongs to `T-A2`), so only direction and entry bar are scoreable. `T-A2` entry
  `14:55`, anchor `14:50`, trigger `28289.75` (`ptb.trigger.bars`, provenance `read`).
- **Which condition fired:** **§5.4.7c**, `invalidated_boundary_close` — a close beyond the Step 2
  boundary against the trade direction. Not §5.4.7a and not §5.4.7b.
- **Triage:** unresolved — and now narrowed to one line of the rulebook.
- **Basis:** §5.4.7c is **M** and unambiguous as written: the close alone is the whole test, with
  no strength qualifier, on *"the one already selected under §2.2.8"*. So the engine is applying
  it correctly, and the open question is upstream — **is the engine's selected boundary the same
  line the trader was trading against?** That is **D-30** (the boundary is the base's close
  extreme on the Step 1 side), a decision `docs/STATE.md` records as chosen from a 6-to-5 corpus
  split that no passage addresses directly. **`T-A2` is the label whose `phase6_scope` records the
  most transcribed reference values** (anchor, trigger, stop and distance all present), so it is
  the one that would score the most if the boundary reading changed. **Nothing is changed here.**

### T-B1 — matched exactly, then suppressed by a gate; and R disagrees by 1.17

- **What the engine did:** anchored `16:45`, filled `16:50` — the label's exact entry bar — at
  trigger `28349.75` and stop `28308.50`, both **+0.00** against the label. L5 then emitted
  `SUPPRESSED`, `blocked_by: into_200`. Its R is `41.25` against the label's `42.42`, a delta of
  **−1.17**.
- **What the label says:** entry `16:50`, trigger `28349.75` (`read`), stop `28308.50` with
  distance `42.42` (`stop.by_ptb_extreme`, derived). The trader **took** this trade.
- **Triage (the gate):** unresolved — a real disagreement, and the most specific one in the run.
- **Basis:** §7.1.4 / **D-31** blocks a long at or below the 200 SMA on fast charts, made
  symmetric by the human on 2026-08-09. The engine reproduced the entry to the cent and then
  declined it on that rule. Nothing in this run says the rule is wrong — but this is the only row
  where a labelled trade was reproduced *and* refused, so it is the one place the corpus and the
  engine can be put against each other on a gate rather than on a price. **n = 1.**
- **Triage (the 1.17):** unresolved — two readings of the same §5.4.1 quantity.
- **Basis:** both sides claim the PTB's opposite extreme (**D-18**, no floor). The engine's R is
  `|fill − stop|` at fill (§5.4.2); the label's distance was derived from the artifact. This is the
  same class as the readings `docs/STATE.md` already records for this label — `T-B1` is the
  corpus's one close agreement between §10.10 and §5.4.1, at 0.99 points. A third figure now
  exists and **no reading is adopted here.**

### NQ3-A1 — excluded by its own declaration

- **What the engine did:** no L3 record in RTH on `2026-08-03` at all (same silent-session
  diagnostic).
- **What the label says:** `circular: true`. `entry.pos_ts` is provenance *derived* — the labeller
  computed it as *"first bar trading above the 28,597.50 trigger"*, which is §5.2.4, the rule the
  engine applies.
- **Triage:** **excluded — nothing is drawn from this row**, matched or not.
- **Basis:** `docs/PHASE6.md` §4. A match here would have tested the labeller's arithmetic, not
  the engine; a miss tests nothing either. §1 carries the CIRCULAR flag on the row itself.

### What did match

Two labels paired on the exact bar, and **both agreed on price exactly, with no tolerance used**:
`LT-B1` (`named`) — anchor bar and trigger `28446.50`, delta `+0.00`, with the engine going on to
fill the order the trader declined, which `docs/PHASE3.md` §2 makes **correct-but-not-taken and
never a false positive**; and `T-B1` (`taken`) — trigger and stop both `+0.00`, discussed above.

**Both matches are exact and both misses are total.** Nothing in this run lands in between — there
is no case of the engine finding the right setup a bar or two late with approximately right prices.
That is a property of these ten instances and **is not projected past them.**

## 3. Emissions not paired to a taken label (n = 25)

**The label set is not exhaustive, so none of these is a false positive.** The four charts record what the trader took and named, never every setup the session offered. A `named` or `no_opportunity` label's own emissions may also appear here, because those classes pair on the entries frame rather than on emissions. No rate is computed from this section and no verdict follows from it (`CLAUDE.md`).

| ts | direction | event | trigger | fill | stop | R |
|---|---|---|---|---|---|---|
| `2026-07-27 02:35:00+00:00` | bullish | suppressed | — | — | — | — |
| `2026-07-27 03:00:00+00:00` | bullish | suppressed | — | — | — | — |
| `2026-07-27 05:05:00+00:00` | bullish | signal | 28656.5 | 28656.5 | 28638.0 | 18.5 |
| `2026-07-27 05:30:00+00:00` | bullish | signal | 28679.75 | 28680.0 | 28672.0 | 8.0 |
| `2026-07-27 05:50:00+00:00` | bullish | signal | 28693.0 | 28693.0 | 28681.25 | 11.75 |
| `2026-07-27 06:30:00+00:00` | bullish | signal | 28704.75 | 28704.75 | 28693.75 | 11.0 |
| `2026-07-27 13:55:00+00:00` | bearish | signal | 28446.5 | 28446.5 | 28504.0 | 57.5 |
| `2026-07-27 15:10:00+00:00` | bearish | signal | 28138.25 | 28138.25 | 28198.5 | 60.25 |
| `2026-07-27 15:25:00+00:00` | bearish | signal | 28062.25 | 28062.25 | 28151.5 | 89.25 |
| `2026-07-30 03:30:00+00:00` | bearish | signal | 27487.5 | 27487.5 | 27523.25 | 35.75 |
| `2026-07-30 03:40:00+00:00` | bearish | signal | 27491.5 | 27491.5 | 27556.75 | 65.25 |
| `2026-07-30 04:00:00+00:00` | bearish | signal | 27517.75 | 27517.75 | 27563.5 | 45.75 |
| `2026-07-31 00:55:00+00:00` | bullish | signal | 28501.75 | 28501.75 | 28468.75 | 33.0 |
| `2026-07-31 02:50:00+00:00` | bearish | suppressed | — | — | — | — |
| `2026-07-31 03:00:00+00:00` | bearish | suppressed | — | — | — | — |
| `2026-07-31 03:10:00+00:00` | bearish | suppressed | — | — | — | — |
| `2026-07-31 04:55:00+00:00` | bullish | signal | 28552.25 | 28552.25 | 28534.5 | 17.75 |
| `2026-07-31 05:40:00+00:00` | bullish | signal | 28561.5 | 28561.5 | 28541.0 | 20.5 |
| `2026-07-31 05:50:00+00:00` | bullish | signal | 28575.75 | 28575.75 | 28548.75 | 27.0 |
| `2026-07-31 06:00:00+00:00` | bullish | signal | 28590.5 | 28590.5 | 28565.25 | 25.25 |
| `2026-07-31 17:45:00+00:00` | bullish | suppressed | — | — | — | — |
| `2026-08-03 02:35:00+00:00` | bullish | signal | 28662.75 | 28663.0 | 28633.75 | 29.25 |
| `2026-08-03 02:50:00+00:00` | bullish | signal | 28650.25 | 28650.25 | 28639.25 | 11.0 |
| `2026-08-03 03:20:00+00:00` | bullish | signal | 28675.5 | 28675.5 | 28657.5 | 18.0 |
| `2026-08-03 05:35:00+00:00` | bearish | suppressed | — | — | — | — |
